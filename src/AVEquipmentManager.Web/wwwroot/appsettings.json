{
  "_comment_apibaseurl": "Must match the API's HTTPS listener URL. After the Phase 3 security patch added UseHttpsRedirection(), hitting the http:// URL produces a 307 redirect that browsers cancel on cross-origin POSTs (the source of the 'TypeError: Failed to fetch' error). Trust the dev cert once with: dotnet dev-certs https --trust",
  "ApiBaseUrl": "https://localhost:7127"
}
